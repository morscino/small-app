import { DbConfig } from './database.js';
import { ServerConfig } from './server.js';

/**
 * config object registers all available configurations
 */
const config = {
  DbConfig,
  ServerConfig,
};

export default config;